import { DashboardShell } from "./dashboard-shell"

export const metadata = {
  title: "Dashboard",
  description: "PAIDS Design System — Dashboard",
}

export default function DashboardPage() {
  return <DashboardShell />
}
